{
  "uuid": "ea07f864-4e7e-475f-b174-857fcb06cd3a",
  "slug": "ad-password-spray",
  "title": "Password Spray Across Many Accounts",
  "description": "Detects a password-spray pattern where one source IP produces failed logons (4625) against many distinct accounts in a short window, testing a few passwords broadly to avoid lockouts.",
  "severity": "high",
  "families": ["siem"],
  "tags": ["credential-access", "brute-force", "password-spray"],
  "status": "active",
  "mitre": [{"techniqueId": "T1110.003", "tacticId": "TA0006"}],
  "rules": ["rules/siem/ad-password-spray.sigma"],
  "intent": {
    "conditions": ["Windows Security log event matching the described pattern"],
    "dataSourceRequirements": ["Windows Security event log"],
    "tuningParameters": {},
    "sourceContext": "Detects a password-spray pattern where one source IP produces failed logons (4625) against many distinct accounts in a short window, testing a few passwords broadly to avoid lockouts."
  }
}
