{
  "uuid": "d9810ed2-c4e1-439e-a46b-3ac92bec3178",
  "slug": "linux-docker-credential-theft",
  "title": "Docker Registry Credential Theft",
  "description": "Detects access to Docker registry credential files (~/.docker/config.json) by copy or read tools, which store base64-encoded registry credentials or auth tokens.",
  "severity": "medium",
  "families": ["siem"],
  "tags": ["credential-access", "linux", "docker"],
  "status": "active",
  "mitre": [{"techniqueId": "T1552.001", "tacticId": "TA0006"}],
  "rules": ["rules/siem/linux-docker-credential-theft.sigma"],
  "intent": {
    "conditions": ["Linux process/audit event matching the described pattern"],
    "dataSourceRequirements": ["Linux process execution logs (auditd/EDR)"],
    "tuningParameters": {},
    "sourceContext": "Detects access to Docker registry credential files (~/.docker/config.json) by copy or read tools, which store base64-encoded registry credentials or auth tokens."
  }
}
