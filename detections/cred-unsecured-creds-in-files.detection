{
  "uuid": "bea40c4d-f2ca-47cf-b4e6-96d7bd70db20",
  "slug": "cred-unsecured-creds-in-files",
  "title": "Credential Hunting in Files via findstr/grep",
  "description": "Detects recursive searches for password, secret, or apikey strings across the filesystem using findstr or grep, a common discovery step to harvest plaintext credentials stored in scripts and config files.",
  "severity": "medium",
  "families": ["siem"],
  "tags": ["credential-access", "unsecured-credentials", "discovery"],
  "status": "active",
  "mitre": [{"techniqueId": "T1552.001", "tacticId": "TA0006"}],
  "rules": ["rules/siem/cred-unsecured-creds-in-files.sigma"],
  "intent": {
    "conditions": ["findstr/grep recursive search for credential keywords"],
    "dataSourceRequirements": ["Process creation telemetry with command line"],
    "tuningParameters": {},
    "sourceContext": "Attackers grep the disk for passwords and keys after gaining a foothold."
  }
}
