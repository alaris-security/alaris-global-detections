{
  "uuid": "14e98611-4d88-4874-9a72-6754451cf736",
  "slug": "cicd-pipeline-env-secret-dump",
  "title": "CI Pipeline Environment Secret Dump",
  "description": "Detects pipeline steps that print the full environment or secret variables (env, printenv, set, echo of secret vars), a technique to exfiltrate CI secrets through build logs.",
  "severity": "high",
  "families": ["siem"],
  "tags": ["credential-access", "cicd", "secrets"],
  "status": "active",
  "mitre": [{"techniqueId": "T1552", "tacticId": "TA0006"}],
  "rules": ["rules/siem/cicd-pipeline-env-secret-dump.sigma"],
  "intent": {
    "conditions": ["CI/CD, IaC, or secrets-store event matching the described pattern"],
    "dataSourceRequirements": ["CI/CD, IaC, secrets-manager, or process logs"],
    "tuningParameters": {},
    "sourceContext": "Detects pipeline steps that print the full environment or secret variables (env, printenv, set, echo of secret vars), a technique to exfiltrate CI secrets through build logs."
  }
}
