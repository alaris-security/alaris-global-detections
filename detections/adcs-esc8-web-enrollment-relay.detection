{
  "uuid": "c7333f7e-cf1f-4b8d-8d39-545e6cd25726",
  "slug": "adcs-esc8-web-enrollment-relay",
  "title": "ADCS ESC8 Web Enrollment NTLM Relay",
  "description": "Detects certificate enrollment via the ADCS web enrollment endpoint (certsrv) authenticated with a machine account over NTLM, the ESC8 technique that relays coerced authentication to mint certificates for a victim.",
  "severity": "critical",
  "families": ["siem"],
  "tags": ["credential-access", "adcs", "ntlm-relay"],
  "status": "active",
  "mitre": [{"techniqueId": "T1649", "tacticId": "TA0006"}],
  "rules": ["rules/siem/adcs-esc8-web-enrollment-relay.sigma"],
  "intent": {
    "conditions": ["Directory/security event matching the described technique"],
    "dataSourceRequirements": ["Active Directory Security logs / ADCS logs"],
    "tuningParameters": {},
    "sourceContext": "Detects certificate enrollment via the ADCS web enrollment endpoint (certsrv) authenticated with a machine account over NTLM, the ESC8 technique that relays coerced authentication to mint certificates for a victim."
  }
}
