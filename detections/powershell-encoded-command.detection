{
  "uuid": "a8626f5e-523c-4d96-bfce-8a47b75e0e90",
  "slug": "powershell-encoded-command",
  "title": "PowerShell EncodedCommand Execution",
  "description": "Detects PowerShell invoked with -EncodedCommand (or the -e/-enc/-ec abbreviations) carrying a base64 payload, a common way to hide the executed script from casual command-line inspection.",
  "severity": "high",
  "families": ["siem"],
  "tags": ["execution", "powershell", "obfuscation"],
  "status": "active",
  "mitre": [{"techniqueId": "T1059.001", "tacticId": "TA0002"}],
  "rules": ["rules/siem/powershell-encoded-command.sigma"],
  "intent": {
    "conditions": ["PowerShell command line or script block matching the described pattern"],
    "dataSourceRequirements": ["PowerShell script-block logging or process command line"],
    "tuningParameters": {},
    "sourceContext": "Detects PowerShell invoked with -EncodedCommand (or the -e/-enc/-ec abbreviations) carrying a base64 payload, a common way to hide the executed script from casual command-line inspection."
  }
}
