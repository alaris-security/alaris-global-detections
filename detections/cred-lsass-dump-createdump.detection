{
  "uuid": "3a408e55-041c-4647-bca3-0f7dd1c77a82",
  "slug": "cred-lsass-dump-createdump",
  "title": "LSASS Dump via createdump.exe",
  "description": "Detects the .NET createdump.exe utility being used to dump the memory of lsass.exe, a living-off-the-land credential-theft method that abuses a legitimate runtime tool.",
  "severity": "critical",
  "families": ["siem"],
  "tags": ["credential-access", "lsass", "createdump"],
  "status": "active",
  "mitre": [{"techniqueId": "T1003.001", "tacticId": "TA0006"}],
  "rules": ["rules/siem/cred-lsass-dump-createdump.sigma"],
  "intent": {
    "conditions": ["Process creation matching the described tool/technique"],
    "dataSourceRequirements": ["Process creation telemetry with command line"],
    "tuningParameters": {},
    "sourceContext": "Detects the .NET createdump.exe utility being used to dump the memory of lsass.exe, a living-off-the-land credential-theft method that abuses a legitimate runtime tool."
  }
}
