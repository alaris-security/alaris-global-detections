{
  "uuid": "1ad7f696-335b-4713-920f-b388daf33941",
  "slug": "collection-rdp-saved-connection-theft",
  "title": "Saved RDP Connection Theft",
  "description": "Detects access to saved Remote Desktop connection files (.rdp, Default.rdp) and cached RDP credentials, used to discover and reuse stored connections for lateral movement.",
  "severity": "medium",
  "families": ["siem"],
  "tags": ["credential-access", "rdp"],
  "status": "active",
  "mitre": [{"techniqueId": "T1552", "tacticId": "TA0006"}],
  "rules": ["rules/siem/collection-rdp-saved-connection-theft.sigma"],
  "intent": {
    "conditions": ["Endpoint or proxy telemetry matching the described collection/exfiltration pattern"],
    "dataSourceRequirements": ["Process telemetry or proxy logs"],
    "tuningParameters": {},
    "sourceContext": "Detects access to saved Remote Desktop connection files (.rdp, Default.rdp) and cached RDP credentials, used to discover and reuse stored connections for lateral movement."
  }
}
