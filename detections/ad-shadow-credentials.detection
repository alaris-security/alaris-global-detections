{
  "uuid": "99ab69dc-3dfe-41b7-bfce-e376db54bc7e",
  "slug": "ad-shadow-credentials",
  "title": "Shadow Credentials on Account (msDS-KeyCredentialLink)",
  "description": "Detects modification of the msDS-KeyCredentialLink attribute (event 5136), the Shadow Credentials technique that adds an attacker-controlled key to an account for certificate-based authentication and persistence.",
  "severity": "high",
  "families": ["siem"],
  "tags": ["persistence", "active-directory", "shadow-credentials"],
  "status": "active",
  "mitre": [{"techniqueId": "T1098", "tacticId": "TA0003"}],
  "rules": ["rules/siem/ad-shadow-credentials.sigma"],
  "intent": {
    "conditions": ["Windows Security log event matching the described pattern"],
    "dataSourceRequirements": ["Windows Security event log"],
    "tuningParameters": {},
    "sourceContext": "Detects modification of the msDS-KeyCredentialLink attribute (event 5136), the Shadow Credentials technique that adds an attacker-controlled key to an account for certificate-based authentication and persistence."
  }
}
