{
  "uuid": "33a36d64-b5bb-4676-8216-ed234eb85a20",
  "slug": "collection-keepass-config-trigger",
  "title": "KeePass Trigger Export Abuse",
  "description": "Detects modification of the KeePass configuration to add an export trigger (KeePass.config.xml with a Trigger that exports the database to CSV/XML), a technique to silently dump a KeePass vault to cleartext.",
  "severity": "high",
  "families": ["siem"],
  "tags": ["credential-access", "keepass"],
  "status": "active",
  "mitre": [{"techniqueId": "T1555.005", "tacticId": "TA0006"}],
  "rules": ["rules/siem/collection-keepass-config-trigger.sigma"],
  "intent": {
    "conditions": ["Endpoint or proxy telemetry matching the described collection/exfiltration pattern"],
    "dataSourceRequirements": ["Process telemetry or proxy logs"],
    "tuningParameters": {},
    "sourceContext": "Detects modification of the KeePass configuration to add an export trigger (KeePass.config.xml with a Trigger that exports the database to CSV/XML), a technique to silently dump a KeePass vault to cleartext."
  }
}
