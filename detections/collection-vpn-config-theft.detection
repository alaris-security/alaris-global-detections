{
  "uuid": "0311fbbe-b447-4754-bc03-0198f577cd07",
  "slug": "collection-vpn-config-theft",
  "title": "VPN Client Configuration Theft",
  "description": "Detects access to VPN client configuration files that may embed credentials (OpenVPN ovpn with auth-user-pass, Cisco AnyConnect profiles, WireGuard conf), used to obtain remote-access credentials.",
  "severity": "medium",
  "families": ["siem"],
  "tags": ["credential-access", "vpn"],
  "status": "active",
  "mitre": [{"techniqueId": "T1552", "tacticId": "TA0006"}],
  "rules": ["rules/siem/collection-vpn-config-theft.sigma"],
  "intent": {
    "conditions": ["Endpoint or proxy telemetry matching the described collection/exfiltration pattern"],
    "dataSourceRequirements": ["Process telemetry or proxy logs"],
    "tuningParameters": {},
    "sourceContext": "Detects access to VPN client configuration files that may embed credentials (OpenVPN ovpn with auth-user-pass, Cisco AnyConnect profiles, WireGuard conf), used to obtain remote-access credentials."
  }
}
