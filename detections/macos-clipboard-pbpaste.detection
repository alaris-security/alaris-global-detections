{
  "uuid": "2d1b6de7-f6c8-4581-8231-63db31b94d66",
  "slug": "macos-clipboard-pbpaste",
  "title": "macOS Clipboard Capture via pbpaste",
  "description": "Detects use of pbpaste to read clipboard contents, potentially in a loop, to harvest copied passwords, tokens, and sensitive text on macOS.",
  "severity": "medium",
  "families": ["siem"],
  "tags": ["collection", "macos", "clipboard"],
  "status": "active",
  "mitre": [{"techniqueId": "T1115", "tacticId": "TA0009"}],
  "rules": ["rules/siem/macos-clipboard-pbpaste.sigma"],
  "intent": {
    "conditions": ["macOS process event matching the described pattern"],
    "dataSourceRequirements": ["macOS process execution logs (ESF/EDR)"],
    "tuningParameters": {},
    "sourceContext": "Detects use of pbpaste to read clipboard contents, potentially in a loop, to harvest copied passwords, tokens, and sensitive text on macOS."
  }
}
